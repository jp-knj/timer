// import Timer from '../models/Timer'

// @description meature the timer
// @route       POST /api/timer
// @access      Private
export const measureTimer = async (req: Request, res: Response): Promise<void> => {
  try {
  } catch (error) {
    throw error
  }
}
